const MatchSpan = ({ text }) => {
    return (
        <span className="bg-like-light text-sm rounded-md px-2 py-1 text-like-dark font-bold">
            {text}
        </span>
    );
};

export default MatchSpan;